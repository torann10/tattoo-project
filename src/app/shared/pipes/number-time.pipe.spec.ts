import { NumberTimePipe } from './number-time.pipe';

describe('NumberTimePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberTimePipe();
    expect(pipe).toBeTruthy();
  });
});
